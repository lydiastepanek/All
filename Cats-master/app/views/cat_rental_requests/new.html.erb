<!DOCTYPE html>

<h1> MAKE A MEOW RENTAL </h1>

<form action="/cat_rental_requests" method= "POST"> -->
  <input type="hidden"
           name="authenticity_token"
           value="<%= form_authenticity_token %>">
<label for="cat_id">Cat</label>
<select name="cat_rental_request[cat_id]" id="cat_id">
  <% Cat.all.each do |c| %>
  <option value="<%= c.id %>" <%= "selected" if @catrental.cat_id == c.id%>>
    <%= c.name %>
  </option>
  <% end %>
</select>
<br>

<label for="start_date">Start Date</label>
<input type="date" name="cat_rental_request[start_date]" id="start_date"
value="<%= @catrental.start_date %>">
<br>

<label for="end_date">End Date</label>
<input type="date" name="cat_rental_request[end_date]" id="end_date"
value="<%= @catrental.end_date %>">

<input type="submit" value="create cat">

</form>
